/*router.post(
  "/pickup/:orderId/schedule",
  auth,
  hasRole("admin"),
  schedulePickup
);

router.post(
  "/pickup/:orderId/proof",
  auth,
  hasRole("admin"),
  upload.single("proof"),
  uploadPickupProof
);

*/